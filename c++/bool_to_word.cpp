/*
Завершите метод, который принимает логическое значение и возвращает строку "Yes" для true или строку "No" для false.
*/

#include <string>

std::string bool_to_word(bool word)
{
  return word ? "Yes" : "No";
}
